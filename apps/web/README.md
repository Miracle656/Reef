# apps/web — Umbra (Next.js)

Next.js (App Router) + Tailwind + `@mysten/dapp-kit` + `@tanstack/react-query`,
GSAP for motion. Walletless sign-in (Enoki/Google), onboarding, home feed,
compose (text + image → Walrus), profile, follow/unfollow. Gasless everywhere.

> Status: not scaffolded yet — UI is intentionally deferred until the contract
> and auth/sponsorship design is reviewed (see root `CLAUDE.md`).
