# apps/mobile — Umbra (Expo)

Expo (React Native) + Expo Router + NativeWind. Uses `@mysten/sui` core + Enoki
for auth (not dapp-kit). Same feature set as web, sharing 100% of `@umbra/core`.

> Status: not scaffolded yet — UI is intentionally deferred until the contract
> and auth/sponsorship design is reviewed (see root `CLAUDE.md`).
