{
  "name": "@umbra/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Sui client, contract bindings, Walrus/Enoki wrappers, Zod schemas. Single source of business logic for web + mobile + indexer.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mysten/deepbook-v3": "^1.5.1",
    "@mysten/enoki": "^1.1.1",
    "@mysten/sui": "^2.19.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
